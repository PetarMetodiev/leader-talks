extends ../base

block vars
	- var title = 'LeaderTalks ' + blogpost.title

block content
	h1 #{blogpost.title}
	img.title-picture(src='#{blogpost.titlePicture}')
	div
		!= blogpost.content
	em #{blogpost.date.toDateString()}